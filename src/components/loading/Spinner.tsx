import { BeatLoader } from "react-spinners";

const Spinner = () => {
    return (
        <div className="spinner-container">
            <BeatLoader color="#DB2677" size={15} />
        </div>
    );
};

export default Spinner;
